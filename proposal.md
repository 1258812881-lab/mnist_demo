

## 手写数字分类项目MNIST

---

### 数据集概述
数据集 – MNIST数字的子集，这是一组由美国高中生和人口调查局员工手写的数字图片，原始数据集有70000个数字的图片，项目老师给出的是子集，4000张图像。这个数据集被广为使用，因此也称为机器学习领域的"Hello World".
>该数据集包含10个类别（数字0到9），共4000张图像（每类400张）。每个特征向量是向量化的图像（784维），包含灰度值[0, 255]。原始图像尺寸为28×28。

老师数据包的相关解释：  
- digits_vec –原始数据集子集 一个 784 × 4000 的矩阵，每一列是一个向量化图像，即特征向量 xi ∈ R^784。
- digits_labels – 类别标签，一个 1 × 4000 的矩阵，包含对应的标签 yi ∈ {0, ..., 9}。
- trainset/testset – 一个 2 × 2000 的矩阵，每一行是用于训练分类器的索引集（是对半分的）

### 可用方法（参考老师提供的方法）
分类问题可用算法：
|算法|思想|
|---|---|
|贝叶斯分类器|贝叶斯分布|
|支持向量机 |用一条线划定决策边界|
| 逻辑回归|输出类别的概率 |
| 感知机| 过误分类驱动更新权重，找到线性决策边界|
| 最邻近| 基准分类器，作为衡量标准对比效果|

- 作为基准，在测试数据上使用简单的最近邻分类器（使用欧氏距离

>AI意见：
对于MNIST手写数字分类任务，在传统机器学习方法中，SVM+RBF核是MNIST分类的最优选择，在你列出的几个方法中，我的推荐排序是：支持向量机（SVM）≈ 逻辑回归 > Fisher判别 > 感知机

-根据课程项目要求，建议：
基线：K邻近（快速验证）
主力：SVM with RBF kernel（追求最佳性能）

---

### 项目提案
根据老师推荐，查找资料，详细计划为：

1. **数据预处理**
归一化：极差标准化方法
数据降维（老师建议）：PCA主成分分析法，会增加项目的复杂度
>对于MNIST数据集，降维之后，每个实例的784个特征会减少为150多个，保留了绝大部分差异性的同时进行了合理压缩，提升算法速度

2. **训练模型**
在训练集上训练模型
主力：**核SVM模型**,可用一对多策略
基准：最邻近
>一对多策略(OvA): 
为每个数字训练1个分类器，总共10个分类器，当需要对一张图片(784个像素值)进行检测分类时，获取每个分类器的决策分数，哪个分类器给分最高，就将其分为哪个类。

3. **调参**
在训练集上，使用5折交叉验证调整超参数：
C(正则化参数)和RbF kernel width
由于有两个参数，可使用网格搜索(grid search)来寻找最优配对，使得交叉验证精确率最高。

4. **模型评估**
在测试集上预测数字，使用混淆矩阵，计算每个数字的精确率，准确率，召回率，F分数

5. **模型评价与总结**
对比降维前后模型的预测效果（基于此上述训练模型调参预测需做两遍，一遍降维，一遍没降）
与基准分类器对比

6. **整理报告与PPT**

---
### 主成分分析
